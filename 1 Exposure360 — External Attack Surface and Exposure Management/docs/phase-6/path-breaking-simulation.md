# Path-Breaking Simulation

The simulator ranks hypothetical relationship removals over an in-memory baseline projection. Results are always `simulation_only=true`, remain bounded to 100 candidates, and are marked `PARTIAL` if baseline traversal truncated.

No database relationship, cloud policy, firewall, IAM binding, DNS record, credential, service, or source system is changed.
