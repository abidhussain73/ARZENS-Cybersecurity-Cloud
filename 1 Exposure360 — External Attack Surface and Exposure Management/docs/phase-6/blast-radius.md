# Blast Radius

Blast radius is bounded reachability context: unique downstream nodes from the traversal result under a profile and hop limit. It deduplicates paths/nodes and categorizes imported applications, identities, data, vulnerabilities, and cloud resources.

Truncated output is partial/lower-bound context, never claimed as complete impact.
