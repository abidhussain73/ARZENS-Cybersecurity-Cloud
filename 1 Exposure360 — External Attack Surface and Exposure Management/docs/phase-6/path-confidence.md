# Path Confidence

Confidence is independent of score. Phase 6 computes geometric mean across edge confidence, and returns edge count, minimum confidence, combined confidence, and low-confidence edge IDs. A weak edge remains explicit rather than being concealed by an arithmetic average.
