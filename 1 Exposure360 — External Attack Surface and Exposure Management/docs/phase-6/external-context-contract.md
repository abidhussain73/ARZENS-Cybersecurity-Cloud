# External Context Contract

The provider-neutral import boundary permits only `IDENTITY`, `CLOUD_RESOURCE`, `APPLICATION`, `DATA`, and `VULNERABILITY`. References are canonical-key based and limited to `EXPOSURE_ASSET` or `EXTERNAL_CONTEXT`.

> Organization context is trusted job input, never a source-payload field.
