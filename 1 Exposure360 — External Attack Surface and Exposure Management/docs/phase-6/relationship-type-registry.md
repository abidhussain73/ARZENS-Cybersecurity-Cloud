# Relationship Type Registry

The immutable version is `relationship-type-registry-v1`. Twenty controlled relationship types cover technical, identity, cloud, application, data, and vulnerability context. Unsupported types and endpoint-kind combinations are rejected.

Registry membership is structural context, not evidence of exploitability.
