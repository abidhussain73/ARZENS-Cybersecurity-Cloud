# Attack-Path Semantics

An attack path is an evidence-backed, bounded graph-theoretic exposure context path. It is not a verified exploit chain, proof of compromise, credential-use result, or source-system change instruction.

This distinction is mandatory for all Phase 6 output surfaces.
