# ADR-0006: Scope Exclusion Precedence

An exclusion always overrides inclusion. Duplicate and redundant seeds are blocking draft conflicts. An exclusion outside all inclusions is a non-blocking warning because it conveys an explicit operator intent without expanding authorization.
