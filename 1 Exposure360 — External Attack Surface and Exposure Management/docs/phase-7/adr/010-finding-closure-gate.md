# ADR 010 — Finding Closure Gate

## Decision

Closure requires immutable verification and closure-decision records generated by the evidence-based gate.

## Rationale

A human resolution claim alone does not prove the condition absent. Missing or degraded evidence yields an unclosed `INCONCLUSIVE` result; condition present yields `DENY_CLOSE`.
