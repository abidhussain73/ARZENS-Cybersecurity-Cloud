# Phase 3 Test Evidence

## Prerequisite Baseline

| Check | Result | Evidence |
|---|---|---|
| Backend Phase 1/2 quality | PASS | Ruff, formatting, strict mypy, and 70 pytest tests passed before Phase 3 work. |
| Frontend Phase 1/2 quality | PASS | TypeScript, ESLint, 9 Vitest tests, production build, and 3 Playwright tests passed before Phase 3 work. |
| AWS Compose runtime | PASS | All accepted services running; live/readiness 200; Phase 2 governance OpenAPI route present; migration head `0003_scope_governance`. |

## Phase 3 Evidence Log

| Task | Command / test | Result | Notes |
|---|---|---|---|
| T021 | `ruff`, format, mypy, `pytest`, Alembic SQL render | PASS | 79 backend tests passed; strict static checks passed; `0004_discovery_staging` rendered 527 lines of additive upgrade SQL from Phase 2 head. |
| T022 | `tests/test_discovery_sources.py` | PASS | Recorded passive-DNS source exposes contract, source health, bounded page checkpoints, Phase 2 normalization, preserved source timestamps, and out-of-scope/malformed warnings. |
| T023 | `tests/test_discovery_sources.py`, `tests/test_candidate_reconciliation.py` | PASS | Recorded certificate metadata emits only scope-relevant SAN/CN and safe wildcard-base candidates, retaining source timestamp/hash and classifying invalid or out-of-scope names. The adapter output is then persisted through reconciliation with asserted `CandidateObservation` source, record key, hash, timestamp, and certificate metadata provenance. |
| T024 | `tests/test_candidate_reconciliation.py` | PASS | One candidate per organization/version/type/canonical key; independent observations preserved; replay remains score-stable; source factors compute 0.86 deterministically; concurrent SQLite conflict-safe insertion leaves one candidate. |
| T025 | `tests/test_discovery_orchestration.py`, `tests/test_discovery_worker_task.py`, `tests/test_jobs.py` | PASS | Job creation pins active approval/content/policy hashes, denies disabled/stopped scopes, creates all stages, uses fenced expiring stage leases, persists candidate results before checkpoints, resumes a multi-page recorded source after interruption, rejects duplicate delivery after completion, preserves four authoritative candidates/observations, and finalizes only after every stage is terminal. `DiscoveryJobWorker` executes configured offline fixtures through the durable stage boundary. The real Celery task body is invoked directly against the SQLite staging schema, verifies the terminal checkpoint and completed job, then verifies idempotent redelivery without duplicate candidates or observations. Durable progress tests prove unknown totals report `indeterminate=true` and `queued=0`, while a known total of `5` with `2` processed reports `indeterminate=false` and `queued=3`. `ruff check`, `ruff format --check`, strict `mypy`, and `pytest` passed with **100 tests**; the modified worker task also passes Ruff and format checks. |
| T026 | `tests/test_dns_validation.py`, `tests/test_discovery_orchestration.py` | PASS | `DnsValidationWorker` uses only injected fixture A/AAAA responses. ScopeGuard runs before resolver calls; out-of-scope, excluded, emergency-stop, blocked-protocol, and outside-schedule paths all assert zero resolver calls. NXDOMAIN and timeout produce bounded non-crashing results. Collection attempts persist answer address, resolved timestamp, TTL, and scope decision. Loopback, private, link-local, metadata-service, IPv6 local, and documentation addresses are rejected for downstream active validation. The configured DNS stage is executed through `DiscoveryJobWorker` and persists truthful known-total progress. Full backend quality gate passed with **114 tests**. |
| T027 | `tests/test_dns_validation.py`, `tests/test_discovery_orchestration.py` | PASS | `TcpValidationWorker` re-authorizes each resolved IP with ScopeGuard, checks immutable protocol/port allowlists and the address-safety classifier, then performs at most one bounded connect. `SocketTcpConnector` uses `socket.create_connection` with the supplied timeout and closes immediately without payload; its no-network monkeypatch test captures the exact timeout. Fixture tests assert zero connector calls for unsupported port/protocol, out-of-scope, excluded IP, documentation/private/loopback, emergency stop, and concurrency exhaustion. Timeout/refusal stay bounded/non-fatal. The configured TCP stage consumes DNS provenance only, persists a terminal checkpoint, reports known-total progress, and skips documentation addresses before connector invocation. Full backend quality gate passed with **127 tests**. |
| T028 | `tests/test_dns_validation.py`, `tests/test_discovery_orchestration.py` | PASS | `TlsMetadataCollector` invokes ScopeGuard before TLS transport, uses only port 443/8443, and denies special/documentation addresses before handshake. Deterministic fixture metadata proves persistence of subject, SANs, validity window, leaf fingerprint, chain length, cipher, protocol, ALPN, and explicit non-verification state. The complete policy, port, schedule, emergency-stop, excluded, out-of-scope, and special-address zero-handshake matrix passes. The configured TLS stage enforces a prior successful TCP attempt, checkpoints each candidate, cooperatively converges cancellation, and bounds transient TLS retries to three numbered attempts before producing one durable dead-letter item. A monkeypatched standard-library TLS connector verifies timeout, exact socket target, SNI, and metadata-only handshake without a live connection. |
| T029 | `tests/test_http_metadata.py`, `tests/test_dns_validation.py`, `tests/test_discovery_orchestration.py` | PASS | Fixture-only HTTP metadata collection validates scheme and credential restrictions, uses transparent minimal headers, bounds sampled bytes, redacts raw cookie values, performs manual redirect authorization before any next-hop call, and returns bounded timeout metadata. HEAD is tried first; only `405`/`501` uses a ranged GET fallback. Relative redirect loops are bounded by `max_redirects` without an additional hop. `HttpMetadataCollector` runs ScopeGuard for the initial URL and every redirect hostname, records a metadata-only collection attempt, and demonstrates that excluded and out-of-scope redirects both receive zero transport calls. The worker consumes `ENDPOINT_HINT` candidates through a durable checkpointed HTTP stage. |
| T030 | `tests/test_discovery_orchestration.py` | PASS | `DiscoveryRecoveryService` persists idempotent CANCELLING state, bounded exponential retry delay, and unique-key dead-letter upsert using the real SQLite discovery staging schema. The worker converges a durable cancellation before any collector call, records retry-scheduled events for bounded transient HTTP and TLS failures, persists three numbered attempts, writes one dead-letter item at exhaustion, and finalizes visibly as `DEGRADED`. A deterministic worker interruption after a committed first checkpoint resumes after lease expiry and completes exactly ten unique staging candidates without duplicates. The affected stage can be requeued idempotently for safe replay. Full Ruff, formatting, strict mypy, and backend regression evidence passes with **151 tests**. |
| T031 | `tests/test_discovery_api.py`, `docs/api/openapi.json` | PASS | FastAPI contract tests cover authorized `202 Accepted` job creation with stable identifier-only queueing, organization-scoped list/detail, truthful unknown-total progress, stages, events, dead letters, durable `202` cancellation, audit actions, unapproved-version denial, and cross-organization denial. The registered API contract was regenerated at `docs/api/openapi.json`. Full Ruff, formatting, strict mypy, and backend regression evidence passes with **154 tests**. |
| T032 | `apps/web/src/DiscoveryJobs.test.tsx`, `apps/web/e2e/discovery-jobs.spec.ts` | PASS | The React discovery interface provides an organization-context connection panel, selectable job list, list/detail lifecycle badges for `QUEUED`, `RUNNING`, `PARTIAL`, `DEGRADED`, `COMPLETED`, `CANCELLING`, `CANCELLED`, and `FAILED`, truthful indeterminate-progress disclosure when totals are unknown, stage counts, safe dead-letter summaries, event log, and an accessible cancellation action. TypeScript, ESLint, all **11** Vitest tests, production Vite build, and **4** Playwright scenarios pass using installed local tooling. |
| AWS runtime | `scripts/phase3_aws_fixture_acceptance.py`, `docs/phase-3/aws-runtime-verification.md` | PASS | After Compose rebuild and migration head verification, the deployed API container executed a self-cleaning recorded-source scenario against AWS PostgreSQL. The actual worker completed the job, persisted **4** staged candidates and **8** job stages, and emitted `{"acceptance":"PASS","job_state":"COMPLETED","worker_result":"completed"}`. No fixture organization remained after cleanup. |
| T031–T032 | Pending | PENDING | API/UI not yet implemented. |

The final report will include a guarded-transport matrix showing zero calls for every denied DNS, TCP, TLS, HTTP, and redirect scenario.

The completed Phase 1/2/3 backend regression at this point reports **151 passed** after Ruff, formatting, and strict mypy passed across 29 source modules.
